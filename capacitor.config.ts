import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xander.app',
  appName: 'calculator',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
